import {
    Outlet,
} from 'react-router-dom'

import {
    Box,
    ButtonGroup,
    Grid,
    SxProps,
} from '@mui/material'

import { NavButton } from "../common/NavButton"

export const styleButtonGroup = {
    width: "100%",
}

<Grid container spacing={2}>
    <Grid item xs={10}>
        <Outlet />
    </Grid>
    <Grid item xs={2}>
        <Box role="navigation" aria-label="Assignments Links">
            <ButtonGroup orientation="vertical" color="primary" sx={styleButtonGroup}>
                <NavButton to="/assignments" match="equals">Overview</NavButton>
                <NavButton to="/assignments/readings">Readings</NavButton>
                <NavButton to="/assignments/project">Project</NavButton>
                <NavButton to="/assignments/statisticslab">Statistics Lab</NavButton>
                <NavButton to="/assignments/exam">Exam</NavButton>
            </ButtonGroup>
        </Box>
    </Grid>
</Grid>
